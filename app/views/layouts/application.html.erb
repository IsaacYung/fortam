<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) + ' | Fortam' : 'Fortam' %></title>
    <meta name="description" content="A Fortam Serviços e Terceirizações oferece profissionais para as suas necessidades, limpeza e conservação, portaria, controle de acesso, recrutamento, mão de obra terceirizada  <%= content_for?(:title) ? '- ' + yield(:title) : '' %>">
    <%= favicon_link_tag 'favicon.ico' %>
    <% cache('application.css') do  %>
      <%= stylesheet_link_tag "application" %>
    <% end %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render 'layouts/header' %>
      <div class="content">
        <%= yield %>
      </div>
      <%= render 'layouts/footer' %>

    <% cache('application.js') do %>
      <link href='https://fonts.googleapis.com/css?family=Ubuntu:400,700' rel='stylesheet' type='text/css'>
      <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
      <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
        ga('create', 'UA-39046515-2', 'auto');
        ga('send', 'pageview');
      </script>
    <% end %>
  </body>
</html>
