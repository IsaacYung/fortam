<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) + ' | Fortam' : 'Fortam - Serviços e Terceirizações' %></title>
    <meta name="description" content="A Fortam oferece profissionais para as suas necessidades, limpeza, conservação, eventos, portaria. Acesse  <%= content_for?(:title) ? '- ' + yield(:title) : '' %>">
    <meta property="og:title" content="<%= content_for?(:title) ? yield(:title) + ' | Fortam' : 'Fortam - Serviços e Terceirizações' %>">
    <meta property="og:site_name" content="Fortam">
    <meta property="og:description" content="A Fortam oferece profissionais para as suas necessidades, limpeza, conservação, eventos, portaria. Clique no post e acese nosso site.">
    <meta property="og:locale" content="pt_BR">
    <meta property="og:image" content="<%= image_url('postfortam.jpg') %>">
    <meta property="og:image:type" content="image/jpeg">
    <meta property="og:image:width" content="800">
    <meta property="og:image:height" content="600">
    <%= favicon_link_tag 'favicon.ico' %>
    <% cache('application.css') do  %>
      <%= stylesheet_link_tag "application" %>
    <% end %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render 'layouts/header' %>
      <div class="content">
        <%= yield %>
      </div>
      <%= render 'layouts/footer' %>

    <% cache('application.js') do %>
      <link href='https://fonts.googleapis.com/css?family=Ubuntu:400,700' async rel='stylesheet' type='text/css'>
      <%= javascript_include_tag "application", async: true, 'data-turbolinks-track' => true %>
      <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
        ga('create', 'UA-39046515-2', 'auto');
        ga('send', 'pageview');
      </script>
    <% end %>
  </body>
</html>
